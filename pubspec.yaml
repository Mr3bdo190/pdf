name: pdf_master
description: A complete PDF editor app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  pdf: ^3.10.7
  syncfusion_flutter_pdf: ^24.2.3
  syncfusion_flutter_pdfviewer: ^24.2.3
  file_picker: ^6.1.1
  path_provider: ^2.1.2
  image_picker: ^1.1.1

flutter:
  uses-material-design: true
